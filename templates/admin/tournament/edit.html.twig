{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
    <div class="row">
        <div class="col-xs-12">
    <h1>Edytuj Turniej</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
            <button type="submit" class="btn btn-primary" formnovalidate>Edytuj</button>
    {{ form_end(edit_form) }}
<br>

            {{ form_start(delete_form) }}
            <button type="submit" class="btn btn-danger" formnovalidate>Skasuj</button>
            {{ form_end(delete_form) }}

        </div>
    </div>
</div>
{% endblock %}
