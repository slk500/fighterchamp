
{{ form_start(form, {
    attr : { 'class': 'ajaxForm' } }) }}


{{ form_row(form.email) }}
        {{ form_row(form.male, {
            label: 'Płeć',
            placeholder : 'Wybierz płeć'
        }) }}
        {{ form_row(form.name, {
            label: 'Imię'
        }) }}
        {{ form_row(form.surname, {
            label: 'Nazwisko'
        }) }}

        {{ form_row(form.club, {
            label: 'Klub (opcjonalnie) - wybierz albo wpisz nowy',
            placeholder: 'Wybierz klub albo wpisz nowy',
            attr: {'class': 'select2'}
        }) }}

{#{% include 'user/_macros/delete_account.html.twig' %}#}

<button style="float:right" type="submit" name="submit" class="btn btn-primary" formnovalidate>
     Zapisz
</button>

{{ form_end(form) }}